{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lifecode.iai.one/schemas/engine-output-v1.json",
  "title": "LifeCodeEngineOutputV1",
  "description": "Khung tối thiểu cho output engine theo README mục 30E — mở rộng field sau khi khóa contract chi tiết.",
  "type": "object",
  "required": ["confidence"],
  "properties": {
    "score": {
      "description": "Điểm hoặc chỉ số tổng hợp layer (nullable khi thiếu dữ liệu)",
      "type": ["number", "null"]
    },
    "confidence": {
      "description": "0..1 — bắt buộc mọi kết luận dùng được",
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "signals": {
      "type": "array",
      "items": { "type": "object", "additionalProperties": true }
    },
    "insights": {
      "type": "array",
      "items": { "type": "object", "additionalProperties": true }
    },
    "recommendations": {
      "type": "array",
      "items": { "type": "object", "additionalProperties": true }
    },
    "data_gaps": {
      "description": "Layer/field thiếu dữ liệu — README: rule thiếu dữ liệu",
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "additionalProperties": true
}
